import Card from 'build/Card';
import Controller from 'build/Controller.js';
import CustomCircleMarker from 'build/CustomCircleMarker';
import CustomDoughnut from 'build/CustomDoughnut';
import CustomMarker from 'build/CustomMarker';
import Filter from 'build/Filter';
import Header from 'build/Header';
import Home from 'build/Home';
import HomeButton from 'build/HomeButton';
import Map from 'build/Map';
import Markers from  'build/Markers';
import PopUpFrame from 'build/PopupFrame';
import Quote from 'build/Quote';
import Table from 'build/Table';

module.exports = {
  Card,
  Controller,
  CustomCircleMarker,
  CustomDoughnut,
  CustomMarker,
  Filter,
  Header,
  Home,
  HomeButton,
  Map,
  Markers,
  PopUpFrame,
  Quote,
  Table,
};

